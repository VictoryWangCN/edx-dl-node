{
  "name": "edx-dl",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "@types/node": "^12.7.1",
    "@types/puppeteer": "^1.19.0",
    "copyfiles": "^2.1.1",
    "puppeteer": "^1.19.0"
  },
  "devDependencies": {},
  "scripts": {
    "generate:resources": "copyfiles ./resources/* ./build/",
    "build": "npm run generate:resources && tsc",
    "dev": "npm run generate:resources && tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc"
  },
  "author": "",
  "license": "ISC"
}
